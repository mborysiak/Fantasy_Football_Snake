# Module Tracker

| Module | Scope | Status | Current Baseline | Next Step |
| --- | --- | --- | --- | --- |
| App database | Consumed `Simulation.sqlite3` and table contracts | active; governed NFFC candidate staged, not promoted | The live `app/Simulation.sqlite3` remains the prior production copy. A full non-promoting 2026 modeling rehearsal built a separate candidate with 351 DK, 385 NFFC, and 328 beta prediction/map rows and passed the Snake DK/NFFC AppTest with zero errors. `SNAKE_SIMULATION_DB` may select that staged candidate without replacing the live database | Retain table-parity and AppTest gates on every refresh; review the completed candidate before any explicit promotion |
| Player pool and ADP | Draftable player loading, ADP simulation, availability, pruning | active | The live canonical cutover uses a 416-row keyed DK source feed. The staged NFFC candidate publishes 497 canonical market entities: 431 keyed offensive players plus 33 `TK` and 33 `TDSP` entities with unique draft-entity keys. Its 385-player model/app population is offense-only; `TK`/`TDSP` rows are filtered before player-key validation. Runtime and displayed ADP share the same canonical keyed value, and normalized display-name joins remain legacy-only | Monitor governed ADP fallbacks and availability leakage audits; keep runtime and displayed ADP on the same canonical key |
| Weekly templates | Historical weekly profile sampling and residual blending | active | Weighted template sampling uses source-owned `template_sample_prob`. V2 production uses `joint_centered_template_v2_v1`: current point draws are constant until the pool-centered donor residual and same weekly path are applied directly. The legacy scaled branch remains only for databases without a V2 handoff. Optional next-year paths use the conditional residual plus explicit appearance mixture | Monitor live-board stability and prospective calibration; do not rescale V2 donors to the zeroed legacy residual spread or re-filter published pools |
| ILP optimizer | Per-pick roster optimization and objective scoring | active | `app/zSim_Helper.py` owns optimizer setup, sampled outcomes, stack bonuses, template residual blend, constraints, straight-serpentine DK turns, and NFFC Third Round Reversal turns | Keep runtime/performance checks close to optimizer changes and add the complete 30-round NFFC kicker/defense roster contract before live use |
| Sequential policy | Non-clairvoyant DK best-ball current-pick recommendations | preview/default | Fresh-session UI default for field testing; opponent-pick count mismatches warn but do not block; empirical next-pick replacement value; default-on symmetric QB-WR/TE stack utility shown separately from raw EV; roster-need-balanced 24-root screen; candidate-consistent noisy-ADP rooms; mutually disjoint 16-season construction, 64-season pilot, 128-season all-candidate decision, and optional 128-season audit banks; legacy ILP fallback unchanged | Re-run the frozen state matrix across stack strengths, then address remaining decision/audit instability and opponent behavior |
| Streamlit UI | Controls, outputs, caching, and interaction flow | active | `app/snake_draft_app.py` wraps helper logic, caches weekly template profile data by DB mtime, keeps DK as the default, and exposes NFFC only when a tagged slice is available with a preview warning | Keep UI text aligned with current sampling/modeling behavior and remove the NFFC warning only after calibrated release validation |
| Research/audits | Calibration and pruning investigations | active | Recent CSV outputs are stored under `research/studies/2026-07-05_best_ball_template_calibration/`; reusable SQL snippets live under `docs/runbooks/queries/` | Promote durable conclusions into the decision log and runbook |
