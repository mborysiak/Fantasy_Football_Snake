# Module Tracker

| Module | Scope | Status | Current Baseline | Next Step |
| --- | --- | --- | --- | --- |
| App database | Consumed `Simulation.sqlite3` and table contracts | active; managed-template schema refresh promoted | The compacted source copy now includes DK/NFFC/beta/NV plus auction-only managed-template center fields. Source and Snake are byte-identical at SHA-256 `27ca20d41296f544932861040c2a4e10cdf7075ef583b1dc249c6f4479493be5`; integrity, foreign keys, zero freelist, full table parity, and DK/NFFC app smokes pass | Retain table-parity, integrity, manifest-hash, and AppTest gates on every refresh |
| Player pool and ADP | Draftable player loading, ADP simulation, availability, pruning | active | The live canonical cutover uses a 416-row keyed DK source feed. The live NFFC adapter publishes 497 canonical market entities: 431 keyed offensive players plus 33 `TK` and 33 `TDSP` entities with unique draft-entity keys. Its 383-player model/app population is offense-only; `TK`/`TDSP` rows are filtered before player-key validation. Runtime and displayed ADP share the same canonical keyed value, and normalized display-name joins remain legacy-only | Monitor governed ADP fallbacks and availability leakage audits; keep runtime and displayed ADP on the same canonical key |
| Weekly templates | Historical weekly profile sampling and residual blending | active; auction-only schema extension ignored | Weighted template sampling uses source-owned `template_sample_prob`. V2 production uses `joint_centered_template_v2_v1`; the legacy scaled branch remains only for databases without a V2 handoff. New `managed_*` columns belong exclusively to Auction and do not alter Snake's exact `week_*`/`active_ppg_resid` sampling. Optional next-year paths retain the conditional residual plus appearance mixture | Monitor live-board stability and prospective calibration; do not consume auction managed centers, rescale V2 donors, or re-filter published pools |
| ILP optimizer | Per-pick roster optimization and objective scoring | active | `app/zSim_Helper.py` owns optimizer setup, sampled outcomes, stack bonuses, template residual blend, constraints, straight-serpentine DK turns, and NFFC Third Round Reversal turns | Keep runtime/performance checks close to optimizer changes and add the complete 30-round NFFC kicker/defense roster contract before live use |
| Sequential policy | Non-clairvoyant best-ball current-pick recommendations | preview/default; DK D256 approved | Fresh sessions use Sequential with 24 rooms and 24 candidates. DK uses an exact nested D256 bank; NFFC remains D128 offense-only. Every Sequential or Legacy click runs in one fresh Python 3.12 subprocess with one inner worker and no retry/fallback. A six-state isolated prelaunch passed at 12.90s parent p90; 33 additional clean fresh states showed D256 +0.621 R512 points, lower regret, zero 24-vs-32 loss, and 12.80s parent p90 before the non-production wide arm invalidated that launch. The clean 81-state held-out 2017-2025 replay then passed every frozen gate: D256 +0.907 points (+0.044%) over D128, 95% CI [-3.160, 5.439], above the -5.196-point noninferiority margin | Keep the Preview label while monitoring live stability; treat Legacy as a fallback/diagnostic, not the approval oracle; rerun sealed evidence after policy, database, runtime, or scoring-contract changes |
| Streamlit UI | Controls, outputs, caching, and interaction flow | active | `app/snake_draft_app.py` wraps helper logic, caches weekly template profile data by DB mtime, keeps DK as the default, and exposes NFFC only when a tagged slice is available with a preview warning. Sequential output shows two unconditional future turns equally weighted across the top five current choices, plus conditional next-pick paths for each choice. True availability remains distinct from roster legality, and future Draft-Now/availability-adjusted edges reuse policy replacement logic without being labeled causal EV. Sequential also displays its active reproducible seed: the normal run repeats it and Run New Seed advances it before rerunning the same marked state. Saved CSVs persist and restore league, team count, and draft position while legacy files default to DK; uploaded settings and player selections remain stable baselines across reruns without overriding later user edits | Keep UI text aligned with current sampling/modeling behavior and remove the NFFC warning only after calibrated release validation |
| Research/audits | Calibration and pruning investigations | active | Recent CSV outputs are stored under `research/studies/2026-07-05_best_ball_template_calibration/`; reusable SQL snippets live under `docs/runbooks/queries/` | Promote durable conclusions into the decision log and runbook |
