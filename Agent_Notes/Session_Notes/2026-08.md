# 2026-08 Session Notes

## 2026-08-14 - Managed-Template Contract Database Refresh

- Installed the rebuilt source-owned `Simulation.sqlite3` after the modeling
  pipeline added auction-only `managed_profile_ppg`,
  `managed_residual_center_ppg`, `managed_active_ppg_resid`, and
  `managed_center_policy` fields for all league templates.
- Snake requires no runtime logic change: it deliberately ignores the new
  managed fields and retains its validated exact `week_*` and legacy/V2
  `active_ppg_resid` sampling semantics.
- Source/Snake SHA-256 is
  `27ca20d41296f544932861040c2a4e10cdf7075ef583b1dc249c6f4479493be5`.
  Full logical parity, SQLite integrity/foreign keys, zero freelist pages, the
  82-test suite, and DK/NFFC Streamlit smokes all passed.

## 2026-08-09 - Sequential Seed Refresh

- Added separate Run Simulation and Run New Seed actions for Sequential Preview.
  The normal action repeats the displayed active seed; the new-seed action
  advances it and immediately reruns the unchanged marked draft state.
- Threaded the selected seed through the sealed one-shot worker request and the
  `FootballSimulation.run_sim` dispatch into the complete Sequential seed
  family. Timing diagnostics report the seed that actually ran.
- Added UI, worker-payload, wrapper, seed-advance, and policy-dispatch coverage.
  The 38-test app suite and syntax checks pass. A real default 24-room DK D256
  AppTest run advanced from 20260719 to 20260720, returned zero exceptions, and
  reported seed 20260720 from the isolated worker.

## 2026-08-07 - Loaded Draft Position Persistence

- Reproduced the delayed reset with a real NFFC AppTest: a saved draft slot
  loaded correctly, survived one unrelated rerun, then reverted to the app
  default of one on the next rerun. Team count followed the same lifecycle.
- The app now retains the uploaded settings row as stable widget defaults while
  applying the saved league only once. User changes continue to win through
  widget state, and a newly uploaded file replaces the prior settings baseline.
- The regression loads NFFC with 14 teams/slot seven, forces two reruns, edits
  the slot to five, and then loads a legacy DK file with 10 teams/slot four.
  Every state transition passed. The full 35-test suite and syntax checks pass.

## 2026-08-07 - Unconditional Future-Pick Planning

- Changed the Next pick and +2 picks tabs from the top current branch to an
  equal-weight aggregation across the five highest-ranked current choices. The
  final tab remains the top three next-pick paths conditional on each choice.
- Captured each rollout choice's existing stack-adjusted Draft-Now Edge and
  added an availability-adjusted edge equal to the selection-rate-weighted
  value. The UI explicitly treats these as policy planning/urgency signals, not
  causal final-roster +EV.
- Added equal-branch-weight and display regressions. The full 35-test suite and
  syntax checks passed. A real isolated 24-room DK run returned the v2 payload,
  five current branches, both unconditional turns, conditional summaries, and
  non-null edge metrics in 11.04 seconds.

## 2026-08-07 - Loaded Selection Persistence

- Reproduced the post-upload reset: Streamlit's data editor stores checkbox
  deltas, but the app applied CSV selections to its input dataframe only once.
  The next widget rerun rebuilt an all-false dataframe, so imported selections
  disappeared before the new edit was applied.
- Imported My Team and Other Team rows now remain the editor input baseline on
  every rerun. Each newly loaded file also increments the editor key once so
  stale checkbox deltas from the prior draft cannot leak into the new file.
- Added a real AppTest that loads one My Team and one Other Team player, forces
  a subsequent widget rerun, and verifies both canonical player-key selections
  remain. The full 34-test suite and syntax checks passed; bare-mode AppTest
  rendered with zero errors and zero exceptions.

## 2026-08-06 - Saved Draft League Round Trip

- Confirmed and hardened the settings-row `League` field in downloaded draft
  CSVs. Save now normalizes the active league before writing it and uses that
  same value for policy metadata.
- Upload normalizes a missing, blank, or null league to DK. Each file is
  processed once by content signature, then the app performs one controlled
  rerun so the saved league is applied before the selector, simulation, and
  player surface are rebuilt.
- Added pure CSV round-trip coverage plus a real Streamlit AppTest that loads an
  NFFC CSV, observes the selector change to NFFC, then loads a legacy CSV with
  no league column and observes the DK fallback. The full 33-test suite passed,
  syntax checks passed, and the bare-mode app rendered with zero errors or
  exceptions.

## 2026-08-05 - Sequential Future-Round Planning Tabs

- Added display-only summaries for the next two user turns from completed
  candidate-consistent Sequential paths. The top current recommendation gets
  next-pick and pick-after tabs; a third tab shows the top three next-pick paths
  for each of the five highest-ranked current candidates.
- Future rows report path selection rate, true room availability after opponent
  picks, selection conditional on availability, roster legality, and completed
  room counts. Players below 10% availability are hidden from the display.
- Passed the compact summary through the one-shot worker boundary without
  serializing research-only path/value matrices. This does not change the root
  screen, rollout policy, D256/D128 decision bank, scoring, or recommendation.
- Focused tests passed, the 30-test app/helper/worker suite passed, and a real
  isolated 24-room DK run returned both future turns plus all 15 conditional
  rows through the worker in 11.24 seconds. A bare-mode Streamlit AppTest
  rendered with zero exceptions and zero errors.
- Follow-up deployment hardening removed the UI's import-time dependency on the
  new availability-floor constant. Streamlit can now hot-reload the updated app
  against a pre-update `zSim_Helper` module still cached in `sys.modules`; it
  uses the same 10% local fallback until a fresh worker payload is available.
- The worker now also embeds one compact future-path JSON payload in its frame
  transport. A new parent restores and removes that hidden transport column; a
  pre-update cached parent preserves it so the updated UI can recover the same
  payload. This closes the mixed-version window without relaxing subprocess
  isolation or duplicating simulation work.
- Moved Sequential output into one immediately visible four-tab set: Current,
  Next pick, +2 picks, and Next pick by current choice. The full 31-test suite
  passed, and a real 24-room Streamlit AppTest rendered all four labels with
  zero errors and zero exceptions.

## 2026-08-03 - DK Sequential D256 Closeout

- Promoted the exact nested D256 decision bank for DK Sequential Preview while
  retaining D128 for NFFC offense-only. Fresh sessions now use Sequential,
  24 rooms, and 24 candidates; custom room counts live under Advanced.
- Added one-shot subprocess isolation for every Sequential and Legacy click.
  Workers use the invoking Python 3.12 environment, force numeric thread counts
  to one, allow no retry or method fallback, and report runtime/package/policy
  metadata. Legacy defaults to 50 simulations and one inner worker.
- A real production smoke completed DK D256 24/24 rooms in 11.72 seconds without
  loading CVXOPT. Legacy completed 50/50 in 7.76 seconds and loaded CVXOPT only
  inside its disposable worker. The focused app/worker/allocator suite passed
  36 tests; the historical adapter suite passed 35 tests.
- The six-state/twelve-worker isolated prelaunch passed every required contract:
  D256 cross-fitted regret mean/max 1.573/7.381 versus D128 3.509/10.174,
  zero positive 24-versus-32 coverage loss, and 12.895-second parent p90.
- The exact fresh 81-state launch failed closed at state 34 when only its
  non-production 32-candidate control arm exited with Windows `0xC0000005` in
  `python312.dll`. Its 33 completed states are diagnostic only: D256 mean
  advantage +0.621 R512 points, clustered CI [-0.062, 1.335], regret mean/max
  0.205/6.581 versus D128 0.826/10.023, zero 24-versus-32 loss, and 12.802-second
  production-arm parent p90.
- The final leakage-safe historical approval excluded Legacy and compared only
  D256 with its exact D128 prefix under common Sequential downstream rooms.
  All 81 states, nine origins, and 3,888 arm-room scores sealed successfully.
  D256 averaged 2,079.373 points versus 2,078.467 for D128: +0.907 points
  (+0.044%), season-clustered 95% CI [-3.160, 5.439] points
  ([-0.150%, 0.262%]), above the preregistered -5.196-point (-0.25%) margin.
  Every frozen gate passed.
- Durable decision: Sequential is the production methodology regardless of
  GLPK/Legacy results. Legacy remains available as a fallback/diagnostic but is
  not an approval oracle. Keep the Preview label for live monitoring.

## 2026-08-02 - Sequential V2 Decision-Bank Stability

- Added a reproducible current-V2 DK study comparing the exact production D128
  allocation with a nested D256 challenger and common policy-inert R512
  reference bank across 27 frozen slot/seed/depth states, 24 rooms, and 24
  candidates.
- All states and legality, completion, identity, physical-state, pool-coverage,
  bank-isolation, and path-invariance contracts passed. The source database
  remained byte-identical and passed full SQLite integrity checks.
- D256 reduced cross-fitted reference score-gap mean/maximum from
  1.4001/10.8227 to 0.1074/4.3025 and reduced gaps above 10 from two to zero.
  Exact R512 winner agreement increased from 11/27 to 17/27.
- Eight recommendations changed; seven improved and one worsened on R512. The
  conditional mean effect was +1.2927 points (+0.0579%), with a three-base-seed
  clustered 95% interval of [0, +2.2599] points. This passes the frozen
  noninferiority gate but is not proof of superiority.
- D256 median policy time was 13.45 seconds versus 13.04 for D128 and 3.61 for
  Legacy. D256 advances only to fresh-seed confirmation; Preview remains
  blocked on rollout runtime, current-V2 24-versus-32 coverage, fresh seeds,
  and later historical forced-pick validation.
- Independent final methodology and artifact reviews found no P0-P2 issue; 17
  focused runtime/V2 handoff tests passed.

## 2026-08-02 - Generated Database Compaction

- The 103.89 MiB working database contained 47.01 MiB of reusable SQLite
  pages; the full 26-table content compacts to about 56.88 MiB, so no runtime
  tables need to be removed for GitHub's hard limit.
- Future production candidates are vacuumed before Snake app smoke and
  promotion, retain exact logical table parity with the source, require zero
  freelist pages, and fail before promotion above 100 MiB.

## 2026-08-03 - Beta Context Source-Copy Promotion

- Modeling run `20260803T040708Z_2075ac47` promoted a fully rebuilt source copy
  after an explicit data-correctness override for beta matcher units. The
  shared database now uses beta V2 matcher context and beta-scored fallback
  centers; the 39 unavailable 2018 QB rows remain donor-ineligible.
- This does not change Snake's supported DK/NFFC scoring paths. The installed
  app database contains 350 DK, 383 NFFC, and 328 beta map rows, passes SQLite
  integrity, matches the promotion-manifest SHA-256, and has exact logical
  table/content parity with the compacted modeling source.
- The governed bare-mode app smoke loaded DK and NFFC with zero errors or
  exceptions. No Snake runtime code change was required for this correction.

## 2026-08-01 - Canonical Current-Team Context

- The modeling source now fills an unassigned Model Inputs/V2 current team
  from the same canonical keyed `Avg_ADPs.team`, backed by
  `player_identity.latest_team`, and publishes `current_team_source`.
- The fallback never overrides an assigned team and never uses a display-name
  join. Snake consumes the source-owned player map without app-side inference.
- DK, NFFC, and beta weekly builds passed against the completed staged annual
  projections before the final governed refresh was started.
- The schema-5 2026 governed release is now live. The Snake database is an
  exact promoted copy of the source simulation database and passed integrity
  plus staged/live hash parity. Current maps contain 350 DK and 383 NFFC
  offensive players; the staged app smoke loaded both leagues with zero errors
  or exceptions.
