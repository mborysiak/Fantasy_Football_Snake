# 2026-08 Session Notes

## 2026-08-02 - Generated Database Compaction

- The 103.89 MiB working database contained 47.01 MiB of reusable SQLite
  pages; the full 26-table content compacts to about 56.88 MiB, so no runtime
  tables need to be removed for GitHub's hard limit.
- Future production candidates are vacuumed before Snake app smoke and
  promotion, retain exact logical table parity with the source, require zero
  freelist pages, and fail before promotion above 100 MiB.

## 2026-08-01 - Canonical Current-Team Context

- The modeling source now fills an unassigned Model Inputs/V2 current team
  from the same canonical keyed `Avg_ADPs.team`, backed by
  `player_identity.latest_team`, and publishes `current_team_source`.
- The fallback never overrides an assigned team and never uses a display-name
  join. Snake consumes the source-owned player map without app-side inference.
- DK, NFFC, and beta weekly builds passed against the completed staged annual
  projections before the final governed refresh was started.
- The schema-5 2026 governed release is now live. The Snake database is an
  exact promoted copy of the source simulation database and passed integrity
  plus staged/live hash parity. Current maps contain 350 DK and 383 NFFC
  offensive players; the staged app smoke loaded both leagues with zero errors
  or exceptions.
