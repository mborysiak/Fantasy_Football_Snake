# 2026-07 Session Notes

## 2026-07-06 - Agent Note System Bootstrap

- Request:
  - review the Daily Fantasy Sims V2 and NBA Sims agent-note structures and set
    up an appropriate system for Fantasy Football plus the Snake app
- Key decisions:
  - used the NBA-style `Agent_Notes/` pattern as the baseline
  - documented this repo as the app/runtime consumer of the modeling repo's
    generated `Simulation.sqlite3`
  - added focused app data-contract and ILP-validation docs
  - moved recent calibration and x-pruning audit outputs into a dated research
    study instead of leaving them in `app/`
- Files changed:
  - `AGENTS.md`
  - `Agent_Notes/README.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/CROSS_REPO_CONTEXT.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
  - `docs/data_contracts/simulation_sqlite_app_contract.md`
  - `docs/runbooks/best_ball_ilp_validation.md`
  - `docs/runbooks/queries/README.md`
  - `docs/runbooks/queries/player_matches.sql`
  - `research/README.md`
  - `research/studies/2026-07-05_best_ball_template_calibration/README.md`
  - `research/studies/2026-07-05_best_ball_template_calibration/results/*.csv`
- Verification:
  - reviewed reference note systems in `Daily_Fantasy_Sims_V2` and `NBA_Sims`
  - no app code executed; documentation/bootstrap task
- Follow-ups:
  - add new dated entries after future app/runtime changes
  - keep app-side research outputs under `research/studies/`

## 2026-07-06 - Best-Ball App Durable Decision Notes

- Request:
  - identify and add any missing notes based on the recent best-ball modeling
    and app work
- Key decisions:
  - promoted weighted template sampling, centered variance-preserving residual
    blending, x-pruning buffer, Streamlit template caching, ADP fallback, and
    query-helper organization into durable notes
  - clarified that the model residual remains the calibrated season-distribution
    anchor while centered template residuals provide player-type/template
    context
- Files changed:
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/MODULE_TRACKER.md`
  - `Agent_Notes/SESSION_NOTES.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
  - `docs/data_contracts/simulation_sqlite_app_contract.md`
  - `docs/runbooks/best_ball_ilp_validation.md`
- Verification:
  - documentation-only update; no app code executed
- Follow-ups:
  - keep calibration changes tied to study evidence under `research/studies/`

## 2026-07-07 - League-Aware Template Loader

- Request:
  - support modeling-side best-ball weekly tables that retain multiple league
    slices in the same copied `Simulation.sqlite3`
- Key decisions:
  - changed the weekly template profile query to join pools to templates on both
    `template_id` and league context when `Best_Ball_Weekly_Templates.league`
    exists
  - kept backward compatibility with older copied databases that do not yet have
    the template `league` column
- Files changed:
  - `app/zSim_Helper.py`
  - `docs/data_contracts/simulation_sqlite_app_contract.md`
  - `docs/runbooks/best_ball_ilp_validation.md`
  - `Agent_Notes/DECISION_LOG.md`
  - `Agent_Notes/Session_Notes/2026-07.md`
- Verification:
  - `python -m py_compile app/zSim_Helper.py`
  - import-level check for `app/zSim_Helper.py`
  - weekly-template cache reads against copied `beta` and `dk` database slices
    after the modeling repo rebuilt/copied `Simulation.sqlite3`

## 2026-07-19 - Sequential Best-Ball Policy Milestone A

- Request:
  - fix decision-dependent availability and perfect-foresight lineup selection
    without drastically slowing current-pick recommendations
- Key decisions:
  - froze the legacy weekly-template/availability/ILP methods with source hashes
  - added tensorized weekly score banks and paired construction/evaluation draws
  - enforced unique, disjoint prediction columns across construction/evaluation
    and exposed an independent evaluation seed
  - added candidate-consistent noisy-ADP draft rooms with global pick removal
  - used greedy marginal best-ball value plus conditional survival/scarcity
  - retained a broad 16-candidate screen; skipped beam/adaptive allocation after
    16 versus 32 candidates showed zero observed omission regret in both the
    opening and derived mid-draft fixtures
  - exposed the corrected method as an explicit Beta and kept legacy ILP as the
    default/fallback
- Verification:
  - frozen legacy hashes, tensor scorer parity, state/information invariants,
    legal/unique paths, and real-database smoke all passed
  - changed the fixture to remove the five physical picks before slot six
  - vectorized roster legality and cached conditional survival by draft turn
  - five matched 24-sample runs produced sequential p50 3.26 seconds versus
    legacy p50 7.09 seconds; all candidate/room paths completed
  - 16 versus 32 candidates had zero observed omission regret in both physical
    states; scarcity improved paired final-roster EV by 28.1 points with an
    approximate paired SE of 11.6
- Evidence:
  - `research/studies/2026-07-19_sequential_best_ball_policy/`
  - `docs/runbooks/best_ball_sequential_policy.md`
- Follow-ups:
  - collect broader Beta/shadow fixtures and use a fresh confirmation bank
    before considering a default flip
  - replace DB-mtime template caching with a modeling-repo build ID when one is
    published

## 2026-07-19 - Initial Sequential Default-Promotion Gate (Superseded)

This receipt records the initial cross-slice run. It was superseded after the
app owner clarified that Snake uses only `dk`; the database `beta` slice belongs
to a separate auction app. Current evidence is recorded in the next receipt.

- Request:
  - add the final correctness gates and broader physical Beta comparisons
    without giving back the policy's runtime gains
- Key decisions:
  - added an optional 128-season confirmation bank with unique columns disjoint
    from construction and evaluation
  - kept confirmation diagnostic-only; its seed/winner cannot alter rollout
    paths, the pilot recommendation, or downstream validation fixtures
  - added an explicit player-pool coverage error and surfaced it cleanly in the
    Streamlit app
  - kept the fixed 10-point quality threshold and legacy runtime comparison
  - retained legacy as default because the promotion gate failed
- Verification:
  - frozen verification suite passed under Python 3.12.10 and the bundled
    Python 3.9.13 runtime
  - all 27 DK states across slots 1/6/12, seeds 17/1017/2017, and rounds 1/8/15
    completed with legal rooms and no errors
  - max 16-versus-32 shortlist regret was 10.71 points; max pilot-confirmation
    regret was 18.01 points; exact confirmation agreement was 16/27
  - Sequential was faster in all opening states but slower in all round-8 and
    round-15 states; overall p50 was 3.10 seconds versus 1.97 for legacy
  - all 27 Beta states were unsupported because the 180-player table cannot
    cover a 12-team, 20-round draft
- Evidence:
  - `research/studies/2026-07-19_sequential_best_ball_release_gate/`
- Follow-ups:
  - deepen or explicitly resize the Beta format, diagnose opening-pick screen
    and pilot noise, profile late-draft rollouts, then repeat the frozen matrix
    with fresh seeds

## 2026-07-19 - DK-Only Sequential Preview Hardening

- Request:
  - lock Snake to DK, retain 24 candidates at every draft depth, and add a
    two-stage pilot-to-decision ranking
- Key decisions:
  - renamed the feature Preview to avoid confusion with the auction app's
    `beta` database slice and locked the Snake UI/release study to `dk`
  - reject incomplete physical draft states based on the exact snake schedule
  - score all 24 roots on a 64-season pilot, then rank the pilot top four on a
    disjoint 128-season operational decision bank
  - keep a fourth 128-season audit bank research-only; its seed cannot change
    the recommendation, rollout paths, or downstream fixtures
  - retain 24 candidates throughout as an explicit runtime-for-coverage choice
- Verification:
  - all four banks were mutually disjoint and seed invariants passed
  - all 27 DK states completed legally with zero execution errors
  - maximum 24-versus-32 omission regret fell to 7.56 points and cleared the
    fixed 10-point shortlist gate
  - decision/audit winners agreed in 18/27 states; two opening states exceeded
    10 points of audit regret, with a maximum of 15.59
  - app-equivalent Preview p50 was 7.60/4.48/2.00 seconds in rounds 1/8/15;
    hidden audit added about 0.12 seconds and is not enabled in the app
  - the live Streamlit app rendered the DK lock, Preview controls, 24-room
    default, and decision-stage recommendation output
- Evidence:
  - `research/studies/2026-07-19_sequential_best_ball_release_gate/`
- Follow-ups:
  - treat the four finalists as an uncertainty tier, retain Preview status, and
    test any predeclared decision-stage change on fresh seeds

## 2026-07-19 - Sequential Preview Field-Test Default

- Request:
  - restart the Snake app and make Sequential the default scoring path
- Decision:
  - made `best_ball_policy` the fresh-session UI default while retaining the
    Preview label and Legacy fallback
  - preserved the failed decision/audit and runtime promotion-gate evidence;
    the default change is an owner-directed field-test choice, not a gate pass
- Verification:
  - app syntax check passed
  - the prior Streamlit process was verified as the Snake app before stopping
  - the restarted app returned a healthy Streamlit endpoint on port 8501

## 2026-07-19 - All-Candidate Decision Scoring

- Request:
  - eliminate the noisy pilot-finalist bottleneck after the Puka/Love draft
    review showed that Rice was excluded and Lamar was promoted by the top-four
    gate
- Decision:
  - keep the 24-root screen and 64-season pilot, but give all 24 completed roots
    the disjoint 128-season operational decision score
  - keep the fourth audit bank research-only and policy-inert
  - retain Sequential Preview as the owner-directed fresh-session default and
    Legacy as fallback while refreshing stability evidence
- Verification:
  - syntax checks and the invariant/real-database verifier passed; all requested
    smoke candidates received both decision and audit scores
  - in the focused Puka Nacua/Jeremiyah Love state, all 24 roots received a
    decision score; Rice ranked second, Olave sixth, and Jackson thirteenth
  - all 27 frozen DK states completed without execution or legality errors and
    maximum 24-versus-32 omission regret remained 7.56 points
  - decision/audit winners agreed in 16/27 states; three regrets exceeded 10
    points and the maximum was 20.27
  - app-equivalent Preview p50 was 8.01/4.97/2.48 seconds in rounds 1/8/15,
    versus legacy at 5.85/1.81/0.56; overall p50 was 3.67 versus 1.81
- Conclusion:
  - retain all-candidate decision scoring because it fixes the observed
    finalist-exclusion failure, while keeping Preview status and Legacy fallback
    because independent evaluation-bank stability and runtime gates still fail

## 2026-07-19 - Empirical Replacement-Aware Draft Timing

- Request:
  - stop early picks from being dominated by late-ADP QBs and use the full
    empirical next-pick availability distribution rather than a 100% cutoff
- Decision:
  - replace raw cross-position rollout priority with positive marginal-value
    drop-off versus the expected best same-position next-pick replacement
  - recalculate replacement value at every user turn from all 1,000 conditional
    ADP samples; retain the construction/decision/audit information boundary
  - allocate the 24-root screen by remaining roster deficits and capacity
- Verification:
  - syntax, synthetic replacement/quota checks, frozen legacy hashes, policy
    invariants, and real-database smoke tests passed
  - the exact 50-room Puka-only round-two fixture used a 3 QB/8 RB/10 WR/3 TE
    root pool; Love ranked first, Rice second, Olave fourth, and the QBs ranked
    20th/22nd/24th; all candidate rooms completed in 9.73 seconds
  - all 27 frozen DK states completed legally with no errors; maximum
    24-versus-32 omission regret was 8.25 and cleared the fixed gate
  - decision/audit winners agreed in 17/27 states; four regrets exceeded 10
    points, with a 12.13 maximum versus 20.27 before this change
  - Preview p50 was 3.85/2.25/1.10 seconds in rounds 1/8/15 versus Legacy at
    2.98/1.12/0.27; the runtime promotion gate still fails
- Conclusion:
  - retain empirical replacement-aware scoring and roster-need root quotas;
    keep Preview status and Legacy fallback while addressing remaining bank
    stability and opponent-model limitations

## 2026-07-19 - Advisory Draft-State Count Mismatch

- Request:
  - allow Sequential Preview to run when the marked opponent-player count does
    not exactly match the snake schedule, because missed selections are hard to
    track late in a live draft
- Decision:
  - replace the hard opponent-pick count error with a visible warning
  - continue from the explicitly marked availability state; if a missed drafted
    player surfaces, mark that player Other Team and rerun
  - preserve hard errors for conflicting selections, missing model players,
    illegal roster state, and insufficient player-pool depth
- Verification:
  - app/helper syntax checks passed
  - the focused real-database verifier passed all policy invariants; a mismatched
    state completed with one advisory warning and an exact state had no warning

## 2026-07-19 - Sequential QB-Pass Catcher Stack Utility

- Request:
  - reward tournament-relevant QB-pass catcher correlation throughout the new
    sequential policy, including QBs paired with earlier pass catchers and late
    pass catchers paired with earlier QBs
- Decision:
  - add symmetric incremental QB-WR/TE stack utility to the root screen, every
    future rollout choice, and empirical next-pick replacement calculation
  - rank completed candidates on a separate decision score equal to raw EV plus
    average final-roster stack utility; keep both components visible side by side
  - enable the preference by default at 20% of combined projected PPG, capped at
    8 per pair and 12 per QB/team so double stacks receive diminishing value
  - retain the individual-template weekly score as raw EV and label stack value
    as heuristic tournament utility rather than calibrated correlated points
- Verification:
  - helper/app syntax checks passed
  - symmetric QB-after-catcher and catcher-after-QB values, second-catcher cap
    behavior, and same-team backup-QB non-duplication checks passed
  - the real-database invariant smoke passed with stack utility enabled and raw
    EV plus average roster utility reconciling to the displayed decision score
  - the 50-room replacement-policy regression passed unchanged with stacking
    disabled, and an app-format smoke exposed Decision Score, Raw EV, Roster
    Stack, Stack Now, and Score vs Best side by side

## 2026-07-20 - NFFC Setup Preview

- Request:
  - allow NFFC Snake setup testing while the real NFFC projection run finishes
- Changes:
  - made the app database filename selectable through `SNAKE_SIMULATION_DB`
    while preserving `Simulation.sqlite3` as the default
  - replaced the hard DK UI lock with DK/NFFC availability detection; DK remains
    the default and NFFC appears only when the selected database has a tagged
    prediction slice
  - added a visible NFFC provenance warning and ignored the generated
    `app/Simulation_nffc_preview.sqlite3`
  - documented that the current isolated preview uses real NFFC ADP but cloned
    DK projection and weekly-template rows
- Verification:
  - helper/app syntax checks passed
  - the NFFC runtime loaded 268 players, 268 weekly pools, and all 16 weekly
    columns; 266 players matched NFFC ADP directly and two deep players used the
    cloned player-map fallback
  - local browser smoke rendered DK and NFFC options, switched to NFFC, showed
    the warning and player grid, and reported no browser errors
- Boundary:
  - DK remains the production and release-gated workflow; NFFC recommendations
    are not valid until calibrated NFFC s3/s4 outputs replace the cloned slices

## 2026-07-20 - NFFC Third Round Reversal

- Request:
  - verify and implement the NFFC Best Ball draft-order reversal after Round 2
- Official rule:
  - the 2026 NFFC $150 and $400 Best Ball Championships are 12-team, 30-round
    Third Round Reversal drafts
  - Round 1 runs 1-12, Rounds 2 and 3 both run 12-1, Round 4 runs 1-12, and the
    remaining rounds alternate
- Changes:
  - made `calculate_snake_picks()` league-aware so only `nffc` uses 3RR and
    `dk` retains the existing straight-serpentine schedule
  - carried the corrected absolute user turns automatically into draft status,
    opponent-count validation, availability horizons, ILP, and sequential
    policy paths through the shared `my_picks` source
  - added an NFFC UI warning that the separate 30-round kicker/team-defense
    roster contract is not yet implemented
- Verification:
  - all 96 picks across eight rounds and 12 slots were unique and complete for
    both DK and NFFC fixtures
  - NFFC round orders passed as `1-12`, `12-1`, `12-1`, `1-12`; slot 12 begins
    `12, 13, 25, 48, 49, 72` and slot 1 begins `1, 24, 36, 37, 60, 61`
  - DK slot fixtures were unchanged and helper/app syntax checks passed

## 2026-07-21 - Streamlit Deployment Dependency Cleanup

- Request:
  - fix Streamlit Community Cloud dependency failures and replace the old
    environment freeze with only the packages the Snake runtime imports
- Changes:
  - reduced `requirements.txt` to the five direct runtime dependencies:
    CVXOPT, NumPy, pandas, SciPy, and Streamlit
  - retained the previously used package versions and normalized the requirements
    file from UTF-16 LE to UTF-8
  - removed unused direct and transitive pins, including the unavailable Deta
    release and the redundant Python 3.10 `backports.zoneinfo` build
- Verification:
  - an isolated Python 3.10.20 install completed successfully
  - Streamlit, pandas, NumPy, SciPy, CVXOPT, `zSim_Helper`, and
    `cvxopt.glpk.ilp` all imported successfully
